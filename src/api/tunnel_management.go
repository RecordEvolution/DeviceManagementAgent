package api

import (
	"context"
	"reagent/messenger"
)

func (ex *External) getAppTunnels(ctx context.Context, response messenger.Result) (*messenger.InvokeResult, error) {
	return nil, nil
}

func (ex *External) getAppPorts(ctx context.Context, response messenger.Result) (*messenger.InvokeResult, error) {
	return nil, nil
}
